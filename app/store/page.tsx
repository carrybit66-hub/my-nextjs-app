export default function StorePage() {
  return (
    <main style={{ padding: 24 }}>
      <h1 style={{ fontSize: 24, fontWeight: "bold" }}>Store</h1>
      <p style={{ marginTop: 12 }}>
        ここは準備中です（本番デプロイ優先のため一旦Supabase依存を外しました）
      </p>
    </main>
  );
}
